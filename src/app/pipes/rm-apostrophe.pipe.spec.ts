import { RmApostrophePipe } from './rm-apostrophe.pipe';

describe('RmApostrophePipe', () => {
  it('create an instance', () => {
    const pipe = new RmApostrophePipe();
    expect(pipe).toBeTruthy();
  });
});
